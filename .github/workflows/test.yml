name: test 
on:
  push:
  
jobs: 
  test: 
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - uses: ./
      env: 
        env_url: 'http://localhost:80'
        dynamic_var: 'dddd'
      with:
        who-to-greet: 'jingzhu'
